{
  "name": "@ytapi/cli",
  "version": "1.0.0",
  "description": "CLI tool to search YouTube and get video metadata",
  "keywords": ["youtube", "search", "cli", "metadata"],
  "repository": {
    "type": "git",
    "url": "https://github.com/ytapis/ytapis"
  },
  "license": "MIT",
  "bin": {
    "ytapi": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup src/index.ts --format cjs --clean",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@ytapi/core": "^1.0.0"
  },
  "devDependencies": {
    "tsup": "^8.4.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
