[build-system]
requires = ["setuptools>=75.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ytapis"
version = "2.0.0"
description = "Search YouTube and get video metadata — no API key required"
readme = "README.md"
license = { text = "MIT" }
keywords = ["youtube", "search", "scraper", "metadata"]
requires-python = ">=3.8"

[[project.authors]]
name = "geethudino (Ruthvik)"
email = "geethudino@pm.me"

[project.urls]
Homepage = "https://github.com/pgboyahpgr-commits/ytapis"
Repository = "https://github.com/pgboyahpgr-commits/ytapis"

[project.scripts]
ytapis = "ytapis.cli:main"

[tool.setuptools.packages.find]
include = ["ytapis*"]
